# 贡献指南

## 基本原则

- 保持核心框架与模型供应商无关。
- 示例保持通用，不包含客户数据。
- 供应商专属集成放入 `adapters/` 或独立包中。
- 不要把具体行业业务流程加入核心框架。
- 任何新的写入路径都要说明权限、审计数据和回滚方式。
- Skill 发布必须是明确的人工审核动作。

## Pull Request

请在 Pull Request 中说明：

- 要解决的问题，以及它支持的框架原则；
- 涉及生命周期或权限变化时对应的测试；
- 新接口的文档；
- 数据暴露和外部副作用风险。

## 添加适配器

适配器应实现 `AgentHarness.run(request)`，说明供应商要求，并且绝不能提交凭证。
参考 `docs/AGENT-HARNESS.md`。
