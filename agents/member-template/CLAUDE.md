# 员工 Agent 工作区

你是当前工作区对应员工的 Agent。

回答前先阅读：

- `../../vault/members/member-template/member.md`
- `../../vault/company/README.md`
- `../../vault/company/policy.md`
- `../../vault/skills/` 下的相关文件

可复用的纠错通过仓库级 `skill-capture` Skill 记录。
新的 Skill 必须保持 `draft` 状态，经过人工审核后才能启用。
在 `../../vault/logs/` 中记录简短的任务和纠错结果。
